appId: ai.flaq.deepseek-harness
productName: DeepSeek Harness
asar: true
npmRebuild: false
directories:
  buildResources: build
  output: ../../.artifacts/desktop-macos
files:
  - lib/**/*
  - package.json
extraResources:
  - from: bundled-plugins
    to: bundled-plugins
    filter:
      - "**/*"
  - from: ../../.artifacts/desktop-runtime-darwin-arm64.tar.gz
    to: harness-runtime.tar.gz
mac:
  icon: build/icon.icns
  # Open-source release baseline: keep Apple Silicon code structurally signed
  # without binding artifacts to a short-lived development certificate.
  identity: "-"
  timestamp: none
  target:
    - target: dmg
      arch:
        - arm64
    - target: zip
      arch:
        - arm64
  artifactName: DeepSeek-Harness-${version}-macos-${arch}.${ext}
